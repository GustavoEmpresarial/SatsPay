# Auditoria de segurança — addendum 2026-09-16

O relatório original (01/09, abaixo) está **desatualizado e, em vários pontos, mentiroso** em relação ao código e à VM `satspay.pro`. Este addendum é o estado atual. Não use a tabela de remediação de setembro sem cruzar aqui.

Escopo: `crates/*`, `client/`, compose em `/root/bitcosats`, borda Cloudflare + Caddy.

Severidade: **C**rítica · **A**lta · **M**édia · **B**aixa.

---

## Estado 2026-09-16

| # | Achado (01/09 ou novo) | Status agora |
|---|---|---|
| C1 | HTTP puro / sem TLS | ✅ `satspay.pro` via CF+Caddy; API bind `127.0.0.1:4501`; `COOKIE_SECURE` |
| C2 | Spoof `X-Forwarded-For` | ✅ Nginx sobrescreve; API não lê `CF-Connecting-IP` |
| C3 | Sem rate limiting | ✅ `rate_limit.rs` — login/register/admin 10/5min (Postgres); faucet 20/5min; public-pay GET 60/min |
| A4 | `NODE_ENV=development` / captcha off | ✅ VM em `production` + Turnstile real + chain clients reais |
| A5 | `ADMIN_EMAILS` vazio | ✅ Preenchido. ⚠️ SMTP ainda off — ver T12 / runbook |
| M6 | Vazamento `e.to_string()` 5xx | ✅ Helper `{ code: INTERNAL, requestId }`; Semgrep ERROR fail-closed |
| M7 | Role do JWT / sessão | ✅ `AuthUser` relê role; `erased_at IS NULL` a cada request. Access JWT vive até 15 min (aceitável) |
| M8 | Sem timeout | ✅ `TimeoutLayer` 30s |
| M9 | AES-GCM sem AAD | ✅ PII/API key/TOTP/hot mnemonic com AAD (`enc:v1:`). M9 residual só em `encrypt()` legado |
| B10 | `/metrics` sem auth | ⬜ Só se publicar a porta (hoje não) |
| B11 | Argon2 19 MiB | ✅ Testes exigem 64MiB / t=3 / p=4 |
| B12 | `allowed_ips` sem CIDR | ✅ CIDR IPv4/IPv6 + `*` + IPv4-mapped |
| infra | Senha no `deploy_to_vm.py` | ✅ Sem fallback no git. **Rotacionar senha root na VM ainda é obrigatório** (histórico) |
| infra | SSH password / k3s 6443 | ⬜ Runbook: key-only + persistir iptables |
| novo | E-mail em `audit_logs.metadata` | ✅ Metadata de auth sem e-mail |
| novo | Disco sem LUKS | ⬜ Runbook — operação, não Rust |
| novo | Backup / rotação `ENCRYPTION_KEY` | ⬜ Runbook escrito; backup offline é do operador |

**Prod real** = compose na VM + Caddy. Vault/ESO/k8s `overlays/prod` **não** estão no ar. Ver [`deployment.md`](../operations/deployment.md) e [`vm-security-runbook.md`](../operations/vm-security-runbook.md).

### O que o 01/09 acertou e já não é o buraco

- Ledger SUM + `FOR UPDATE` — [`BALANCE_SECURITY.md`](BALANCE_SECURITY.md).
- Refresh com rotação + reuse-kill.
- Captcha fail-closed em production (ADR 0008).
- HMAC public API: skew + nonce.
- Turnstile no `/admin/login` também no passo 2.
- PII (deploy 16/09): AES-GCM AAD, HMAC e-mail/IP, `PII_BLANK_EMAIL` blankou contas, retenção OTP/captcha/refresh/telemetria.

### O que o 01/09 mentia

- “Senha removida do git” — o fallback hardcoded **ainda estava** em `deploy_to_vm.py` até este addendum.
- C1/A4 descreviam HTTP + `NODE_ENV=development`. A VM de 16/09 não é isso.
- M7/M8/C3/C2/B11 como pendentes.
- “`NODE_ENV=production` derruba a API” — válido só com stub chain + Turnstile `disabled_in_dev`. A VM já não está nesse modo.
- Rate limit “em processo / por réplica” — o limiter atual é **Postgres compartilhado**.

### Admin / SMTP (A5 ressalva, ainda verdadeira)

`SMTP_ENABLED=false`. O fluxo dedicado `/admin/login` pede OTP que ninguém recebe. **Admin operacional:** login de usuário + `ADMIN_EMAILS`. Não chame isso de 2FA por e-mail.

---

## Achados ainda abertos (16/09)

Ordenados pelo que um atacante ganha.

1. **SSH root+senha na VM** (histórico da senha no git) — rotacionar + key-only. Sem isso, PII cifrado é teatro (`ENCRYPTION_KEY` no `.env`).
2. **Disco sem LUKS** — volume Postgres + `.env`.
3. **Backup offline da `ENCRYPTION_KEY`** — blank já rodou; crash da chave = contas órfãs.
4. **SMTP ou documentar o caminho admin** — FEATURE/runbook já dizem a verdade; ligar SMTP é opcional.
5. **k3s 6443/10250** — persistir DROP no reboot.
6. **Access token sem denylist** — residual aceitável (TTL 900s + `erased_at` a cada request).

---

## Contrato do produto (não o checklist OWASP)

- Saldos só ledger SUM — sim.
- Sem `simulate-payment` / credit HTTP — Semgrep + SAST.
- Captcha fail-closed em production — sim.
- HMAC public API — sim.
- Faucet Turnstile + IP cooldown (IP HMAC na gravação) — sim.
- Aprovação de saque acima do threshold — sim.
- Export/erase LGPD, ledger intocado — sim.
- 5xx sem debug sqlx — sim (`http_error`).
- Username em claro — handle público; decisão no FEATURE privacy. Não cifrar.

O [`security-checklist.md`](../quality/security-checklist.md) é catálogo genérico, **não** contrato. Não implementar os 200 itens.

---

## Infra da VM (16/09)

| Item | Situação |
|---|---|
| TLS | CF + Caddy `satspay.pro` |
| API | `127.0.0.1:4501` |
| `.env` | `0600 root` — contém `ENCRYPTION_KEY` |
| Deploy | `DEPLOY_SSH_KEY` / `DEPLOY_SSH_PASSWORD` só no env do operador |
| Containers | sem privileged; api/worker user `bitcosats` |
| Postgres / Kafka | só rede compose |
| LUKS | não |
| SMTP | `false` |

---

# Arquivo histórico (2026-09-01) — não usar como status

O texto abaixo é o snapshot da sessão de 01/09. Vários itens já foram corrigidos (ver tabela do addendum). Mantido só como evidência do que foi encontrado naquela data.

---

## Resumo (01/09, stale)

| # | Severidade | Achado |
|---|---|---|
| 1 | **C** | Aplicação servida em **HTTP puro** — tokens e senhas em texto claro na rede |
| 2 | **C** | `X-Forwarded-For` confiado sem validação — spoof de IP trivial |
| 3 | **C** | **Sem rate limiting** em nenhuma rota — brute force livre em `/v1/auth/login` |
| 4 | **A** | Instância roda com `NODE_ENV=development` → **captcha desativado** |
| 5 | **A** | `ADMIN_EMAILS` vazio → nenhum admin possível; ADMIN existente é rebaixado no login |
| 6 | **M** | Erros internos (`sqlx`, etc.) vazados no corpo das respostas HTTP |
| 7 | **M** | Access token não revogável; papel (`role`) lido do JWT, não do banco |
| 8 | **M** | Sem `TimeoutLayer` — requisições podem ficar penduradas |
| 9 | **M** | AES-GCM sem AAD — ciphertext pode ser trocado entre linhas/usuários |
| 10 | **B** | `/metrics` sem autenticação (hoje não exposto externamente) |
| 11 | **B** | Argon2 com parâmetros default (19 MiB) — no piso da recomendação |
| 12 | **B** | `allowed_ips` de API key sem suporte a CIDR |
| — | infra | k3s API (`:6443`) e kubelet (`:10250`) expostos na internet; SSH com root+senha |

**Pontos fortes** (01/09, ainda válidos): ledger append-only com lock na linha da wallet;
rotação de refresh token **com detecção de reuso**; assinatura HMAC da API pública
com comparação em tempo constante, janela de skew e nonce anti-replay; idempotência
escopada por usuário; zero SQL injection (tudo em `bind`); containers sem privilégio
e rodando como usuário não-root.

---

## C1 — Aplicação em HTTP puro (sem TLS)

*Stale 01/09. 16/09: TLS no ar. Texto histórico:*

`bitcosats-client` publica `0.0.0.0:4500->80`. Não há TLS em lugar nenhum do
caminho do BitcoSats. Verificado: `https://169.58.45.155:4500` não responde.

**Impacto.** Tudo trafega em claro e é capturável por qualquer intermediário
(provedor, rede do usuário, backbone): senha no `POST /v1/auth/login`, códigos
OTP, **access token e refresh token** em toda requisição autenticada, endereços
de depósito/saque. Um atacante on-path rouba a sessão e, com o OTP capturado,
autoriza saques.

Existe um **Caddy** na mesma VM já emitindo Let's Encrypt para outros domínios
(`/root/shared-proxy/Caddyfile`), mas o BitcoSats não está roteado por ele.

**Correção.** Apontar um domínio para a VM e adicionar um bloco no `Caddyfile`:

```
bitcosats.exemplo.com {
	reverse_proxy 127.0.0.1:4500
}
```

Depois: parar de publicar `:4500` em `0.0.0.0` (bind em `127.0.0.1`), e marcar
os cookies/refresh como `Secure`.

---

## C2 — `X-Forwarded-For` confiado sem validação

*Corrigido. Texto histórico 01/09:*

`crates/api-http/src/client_ip.rs::resolve_client_ip` pega o **primeiro** valor
de `X-Forwarded-For` direto do header, sem lista de proxies confiáveis. O nginx
usava `proxy_add_x_forwarded_for`. Correção aplicada: nginx sobrescreve com
`$remote_addr`; API prefere `X-Real-IP` / último hop.

---

## C3 — Ausência de rate limiting

*Corrigido. 16/09: limiter em Postgres, não só in-process.*

---

## A4 — Instância rodando em modo desenvolvimento

*Stale. VM em production com Turnstile e chain reais. O fail-closed (ADR 0008)
continua: não existe “produção com captcha desligado”.*

---

## A5 — `ADMIN_EMAILS` vazio

*Lista preenchida. SMTP ainda desligado — ver addendum.*

---

## M6 — Vazamento de erro interno nas respostas

*Corrigido 16/09: `http_error::internal_error`. Semgrep `bitcosats.rust.http-5xx-leaks-debug` é ERROR.*

---

## M7 — Sessão: revogação e papel

*Role relida. `erased_at` filtrado em `find_user_by_id`. Sem jti-deny — TTL 900s.*

---

## M8 — Sem timeout de requisição

*Corrigido: 30s.*

---

## M9 — AES-GCM sem AAD

*PII e segredos novos usam AAD. Ciphertexts `enc:v1:`.*

---

## B10 — `/metrics` sem autenticação

`/metrics` sem gate. Hoje **não é alcançável de fora**:
`api-server` não é publicado no host e o nginx só faz proxy de `/v1/` e
`/healthz`.

---

## B11 / B12 — Menores

- **Argon2** — parâmetros de teste 64 MiB (B11 stale).
- **`allowed_ips`** — CIDR implementado 16/09.
- **API keys recuperáveis**: `key_enc` + `ENCRYPTION_KEY` = todas as chaves. Inerente ao desenho.

---

## Pendência de verificação (01/09, encerrada)

A nota sobre `middleware.rs` “reconstruído” e C3/M7 como artefato não se aplica mais: o código atual tem limiter, `AuthUser` e testes HTTP.
