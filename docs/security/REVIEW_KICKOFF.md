# Security review kickoff

Branch vehicle for `/review-security` against the full SatsPay monorepo
(auth, ledger, withdrawals, faucet, public HMAC, OAuth, crypto, custody).

This file is intentional noise so the branch has a non-empty diff vs `main`.
