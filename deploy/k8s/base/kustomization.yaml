apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: bitcosats
resources:
  - api-server
  - worker
  # TODO: outbox-relay/ (hoje uma task dentro do binário worker, não um
  # Deployment separado — ver crates/worker/src/main.rs), frontend/,
  # ingress/ (ver third-party/cert-manager + base/ingress para TLS).
