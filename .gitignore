# Local secrets & wallet backups
secrets/
SEED_BACKUP.txt
SEED_BACKUP.json
hd_wallets.json
master_wallet_backup.json
seed_phrase_backup.txt
*.key
*.pem
*.p12
*.pfx

# Env files (keep examples)
.env
.env.*
!.env.example
!.env.*.example
!**/.env.example

# Local credential stores / deploy helpers (never commit)
deploy.credentials.py
**/deploy.credentials.py

# Dependencies and build artifacts
node_modules/
dist/
target/
# Keep Cargo.lock for reproducible app builds
# Cargo.lock

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store

# Logs and temp
*.log
/tmp/
storage/tmp/
storage/backups/
storage/logs/

# Coverage / llvm
coverage/
lcov.info
*.profraw
.security-audit/

# Playwright (e2e runs against a live base URL and drops artifacts here)
client/test-results/
client/playwright-report/
